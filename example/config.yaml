app:
  web:
    enabled: true
    host: :8080
  grpc:
    enabled: false

sql:
  default:
    enabled: false
    driver: postgres
    dsn: $SQL_DSN

  master:
    enabled: false
    driver: postgres
    dsn: $SQL_DSN

  transactions:
    enabled: false
    driver: postgres
    dsn: $SQL_DSN 

others:
  key: $VALUE
