app:
  web:
    enabled: true
    host: :8080
  grpc:
    enabled: false

sql:
  default:
    enabled: true
    driver: postgres
    dsn: $SQL_DSN

  master:
    enabled: true
    driver: postgres
    dsn: $SQL_DSN

  transactions:
    enabled: true
    driver: postgres
    dsn: $SQL_DSN 

others:
  key: $VALUE
